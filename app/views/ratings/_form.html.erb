<%= form_with(model: rating, local: true) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
        <% rating.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game %>
    <%= form.text_field :game %>
  </div>

  <div class="field">
    <%= form.label :critic_score %>
    <%= form.text_field :critic_score %>
  </div>

  <div class="field">
    <%= form.label :user_score %>
    <%= form.text_field :user_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
