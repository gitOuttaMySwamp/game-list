<%= form_with(model: genre_game, local: true) do |form| %>
  <% if genre_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genre_game.errors.count, "error") %> prohibited this genre_game from being saved:</h2>

      <ul>
        <% genre_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game %>
    <%= form.text_field :game %>
  </div>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
